An audio visualizer which reacts to the frequencies of the audio using Fast Fourier Transform and applying the variables to process a colourful visualizer.
Uses a compression form to convert audio into a text-based wrapper in order to optimize performance for shorter length audio files, switches to the original audio file playback in case of longer time lengths.
